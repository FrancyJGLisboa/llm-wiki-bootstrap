---
name: "source-command-init"
description: "Alias for /ctx-init. Scaffold raw/, wiki/, .Codex/commands/, AGENTS.md, log.md, README.md (idempotent)."
---

# source-command-init

Use this skill when the user asks to run the migrated source command `init`.

## Command Template

This is a short-form alias for `/ctx-init`. Read the canonical procedure in `.Codex/commands/ctx-init.md` and execute it verbatim. Do not duplicate the logic here — the canonical file is the single source of truth for the scaffold operation.
