---
name: "source-command-discover"
description: "Alias for /ctx-discover. Surface non-obvious structure — causal chains, hubs, the widest connection, tensions, open questions."
---

# source-command-discover

Use this skill when the user asks to run the migrated source command `discover`.

## Command Template

This is a short-form alias for `/ctx-discover`. Read the canonical procedure in `.Codex/commands/ctx-discover.md` and execute it verbatim. Do not duplicate the logic here — the canonical file is the single source of truth for the discovery report (KG materialization, the three lenses, the tensions/open-questions dashboards, and the read-only guarantee).
