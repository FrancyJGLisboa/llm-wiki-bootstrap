# Start with a local compiler

Choose an empty folder. Context Workspace installs the generic compiler and lets you
activate the included client-decision specialization without opening a terminal.

If that specialization does not fit, choose **Create Specialization**. Describe the work
in ordinary language, inspect realistic example behavior, and approve it. The agent owns
the implementation and activation; you own the domain meaning.
