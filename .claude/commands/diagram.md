---
description: Alias for /wiki-diagram. Synthesize an audience-targeted diagram from an intent by reasoning over the wiki. Optionally render to --pdf or --png.
allowed-tools: Bash, Read, Glob, Grep
argument-hint: "<what you want to show, and for whom>" [--pdf|--png]
---

This is a short-form alias for `/wiki-diagram`. Read the canonical procedure in `.claude/commands/wiki-diagram.md` and execute it verbatim against `$ARGUMENTS`. Do not duplicate the logic here — the canonical file is the single source of truth for retrieval, archetype scoring, the candidate menu, and HTML generation.
