---
description: Alias for /wiki-new. Generate a new domain-shaped wiki and register it in the workspace catalog.
allowed-tools: Bash, Read, Write, Edit, Glob
argument-hint: <name> --domain "<description>" [--workspace <dir>] [--target <path>]
---

This is a short-form alias for `/wiki-new`. Read the canonical procedure in `.claude/commands/wiki-new.md` and execute it verbatim with `$ARGUMENTS`. Do not duplicate the logic here — the canonical file is the single source of truth.
