---
description: Alias for /wiki-registry. List every wiki generated by the factory (name, domain, seeded status, drift).
allowed-tools: Bash, Read
argument-hint: [--workspace <dir>] [prune [--apply]]
---

This is a short-form alias for `/wiki-registry`. Read the canonical procedure in `.claude/commands/wiki-registry.md` and execute it verbatim with `$ARGUMENTS`. Do not duplicate the logic here — the canonical file is the single source of truth.
