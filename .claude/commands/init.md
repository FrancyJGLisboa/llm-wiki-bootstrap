---
description: Alias for /wiki-init. Scaffold raw/, wiki/, .claude/commands/, AGENTS.md, log.md, README.md (idempotent).
allowed-tools: Bash, Read, Write
argument-hint:
---

This is a short-form alias for `/wiki-init`. Read the canonical procedure in `.claude/commands/wiki-init.md` and execute it verbatim. Do not duplicate the logic here — the canonical file is the single source of truth for the scaffold operation.
