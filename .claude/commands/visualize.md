---
description: Alias for /wiki-visualize. Render the wiki as a graph, slides, or mermaid images, or serve it locally.
allowed-tools: Bash, Read, Glob
argument-hint: [graph|mermaid|slides|serve] [target] [--out <path>]
---

This is a short-form alias for `/wiki-visualize`. Read the canonical procedure in `.claude/commands/wiki-visualize.md` and execute it verbatim against `$ARGUMENTS`. Do not duplicate the logic here — the canonical file is the single source of truth for subcommand dispatch, tool-presence checks, and output reporting.
