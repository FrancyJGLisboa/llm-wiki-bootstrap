---
description: Alias for /wiki-lint. Health-check the wiki (broken links, orphans, contradictions, stale claims, gaps).
allowed-tools: Bash, Read, Write, Edit
argument-hint: [--apply]
---

This is a short-form alias for `/wiki-lint`. Read the canonical procedure in `.claude/commands/wiki-lint.md` and execute it verbatim against `$ARGUMENTS`. Do not duplicate the logic here — the canonical file is the single source of truth for what the lint checks and how `--apply` writes fixes.
