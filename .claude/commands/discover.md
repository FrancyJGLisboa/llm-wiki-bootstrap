---
description: Alias for /wiki-discover. Surface non-obvious structure — causal chains, hubs, the widest connection, tensions, open questions.
allowed-tools: Bash, Read
argument-hint: (no args)
---

This is a short-form alias for `/wiki-discover`. Read the canonical procedure in `.claude/commands/wiki-discover.md` and execute it verbatim. Do not duplicate the logic here — the canonical file is the single source of truth for the discovery report (KG materialization, the three lenses, the tensions/open-questions dashboards, and the read-only guarantee).
