---
description: Alias for /ctx-flashcards. Export ## Flashcards sections to an Anki-importable CSV.
allowed-tools: Bash, Read, Glob
argument-hint: [target-dir] [--out <path>]
---

This is a short-form alias for `/ctx-flashcards`. Read the canonical procedure in `.claude/commands/ctx-flashcards.md` and execute it verbatim against `$ARGUMENTS`. Do not duplicate the logic here — the canonical file is the single source of truth for argument parsing, the underlying script invocation, and output reporting.
