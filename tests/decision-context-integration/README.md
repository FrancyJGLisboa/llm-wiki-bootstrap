# Decision-context integration fixture

`scripts/verify-decision-context-integration.sh` builds its fixture in a temporary
directory. It proves explicit profile activation, generic fallback, anchored claim
validation, byte-identical state projection, and source-scoped shards without placing
gold answers or generated context in the repository.
